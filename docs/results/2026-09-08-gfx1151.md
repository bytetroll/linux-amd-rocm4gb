# gfx1151 qualification — 2026-09-08

## Environment

- AMD Ryzen AI MAX+ / Radeon 8060S (`gfx1151`)
- Linux `6.17.0-1024-oem`
- TheRock ROCm 7.14 runtime
- llama.cpp b10469, commit `666f8898a`
- 96 GiB GPU-visible VRAM; 31.1 GiB Linux-visible RAM
- `/sys/module/amdgpu/parameters/no_system_mem_limit=N`

The generic front end was rebuilt from the exact pinned commit with the staged
mmap patch. It dynamically loaded the unchanged b10469 HIP backend; that plugin
had SHA-256
`d99bd8037e64c4b1b738f61cc2fe9736fdc4c86a34e5811e972ab12a281b9a29`.

## Qwen3.6-35B-A3B Q8_0

Command characteristics:

```text
--load-mode mmap --fit off -ngl 999 --no-repack
--ctx-size 512 --predict 8 --no-warmup --verbose
```

Results:

| Measurement | Result |
|---|---:|
| File bytes | 36,903,140,320 |
| GPU layers | 41/41 |
| ROCm model buffer | 34,667.78 MiB |
| CPU-mapped model buffer | 515.31 MiB |
| End-to-end wall time | 11.79 s |
| Maximum RSS | 695,856 KiB |
| Peak VRAM | 35.555 GiB |
| Peak GTT | 0.216 GiB |
| Prompt throughput | 85.8 t/s |
| Generation throughput | 35.9 t/s |
| Peak Tctl | 62.875 °C (independent 250 ms sampler) |
| Exit | 0 |
| New kernel fault/SVM lines | 0 |

The log proved full offload, forced mmap, and asynchronous upload for ROCm0.
The model generated eight deterministic tokens and exited. This is the exact
size class that previously entered the KFD restore loop.

## Qwen3-Coder-Next 80B Q8

The complete three-shard set contained 84,812,058,080 bytes (78.99 GiB) and
843 tensors. The same forced-mmap flags and a 512-token qualification context
were used from local NVMe.

| Measurement | Result |
|---|---:|
| GPU layers | 49/49 |
| ROCm model buffer | 80,562.07 MiB |
| CPU-mapped model buffer | 315.30 MiB |
| End-to-end wall time | 22.83 s |
| Maximum RSS | 691,576 KiB |
| Peak VRAM | 80.951 GiB |
| Peak GTT | 0.344 GiB |
| Prompt throughput | 72.9 t/s |
| Generation throughput | 23.6 t/s |
| Peak Tctl | 70.875 °C |
| Exit | 0 |
| New kernel fault/SVM lines | 0 |

All 49 layers were offloaded, the ROCm async staging marker was present,
generation completed, and `/dev/kfd` was released normally.

## GPT-OSS-120B Fable-5 Q5

The single 80,689,581,856-byte GGUF (75.15 GiB) was read from an external
rotating disk. The same forced-mmap flags and a 512-token qualification context
were used.

| Measurement | Result |
|---|---:|
| GPU layers | 37/37 |
| ROCm model buffer | 76,558.74 MiB |
| CPU-mapped model buffer | 379.71 MiB |
| End-to-end wall time | 34:45.75 (storage-bound) |
| Maximum RSS | 613,340 KiB |
| Peak VRAM | 76.960 GiB |
| Peak GTT | 0.353 GiB |
| Peak GPU busy | 78% |
| Prompt throughput | 61.30 t/s |
| Generation throughput | 31.15 t/s |
| Peak Tctl | 75.000 °C |
| Model exit | 0 |
| New kernel fault/SVM lines | 0 |

The model generated all eight requested tokens, returned VRAM and GTT to their
starting baselines, and released `/dev/kfd`. The original 1,800-second GNU
`timeout` supervisor was paused before expiry so the healthy but slow disk read
could finish under the still-active independent 90 °C thermal guard. Resuming
the expired supervisor consequently returned 124, while its child
`/usr/bin/time` recorded the model's actual exit status as 0. This supervisor
artifact is retained here explicitly; it is not a model or ROCm failure.
